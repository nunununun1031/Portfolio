import { Avatar } from "@material-ui/core";
import React, { memo } from "react";

const ImageIcon: React.VFC = memo((props) => {
  return <div>{/* <Avatar src={imageSrc}/> */}</div>;
});

export default ImageIcon;
